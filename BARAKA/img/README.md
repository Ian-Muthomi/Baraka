# Product Images Folder
Place custom product images in this folder and reference them by filename in the product's `image` property in app.js (or upload via ERP product modal).

Example:
```javascript
{ 
  id: 'my-product', 
  name: 'My Custom Product',
  image: 'img/my-custom-image.jpg'
}
```
